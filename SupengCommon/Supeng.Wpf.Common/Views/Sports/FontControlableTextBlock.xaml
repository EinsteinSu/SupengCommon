<UserControl x:Class="Supeng.Wpf.Common.Views.Sports.FontControlableTextBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Supeng.Wpf.Common.Views.Sports"
             xmlns:commons="clr-namespace:Supeng.Wpf.Common.Commons"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" >
    <UserControl.InputBindings>
        <KeyBinding Key="S" Modifiers="Control"  Command="{Binding ShowOrHideCommand}"/>
    </UserControl.InputBindings>
    <StackPanel>
        <Button Command="{Binding SetCommand}" >Font Configuration</Button>
        <TextBlock Text="{Binding Text,Mode=TwoWay}" FontSize="{Binding FontSize,Mode= TwoWay}" Foreground="{Binding Foreground}" FontFamily="{Binding FontFamily}"></TextBlock>
    </StackPanel>
</UserControl>
